{
  "name": "ts-config",
  "version": "0.0.0-development",
  "description": "Extendable, well-designed configurations for the latest versions of TSLint and TypeScript.",
  "license": "MIT",
  "author": "Clemens Akens <clebert@me.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/clebert/ts-config.git"
  },
  "scripts": {
    "prepublishOnly": "./scripts/prepublish.js",
    "release": "semantic-release pre && npm publish && semantic-release post",
    "test": "npm run test:tslint && npm run test:tslint-project && npm run test:typescript",
    "test:tslint": "tslint --config tslint.json test.ts",
    "test:tslint-project": "tslint --config tslint.json --project tsconfig.json test.ts",
    "test:typescript": "tsc --project ."
  },
  "devDependencies": {
    "semantic-release": "~11.0.2",
    "tslint": "~5.8.0",
    "typescript": "~2.6.1"
  },
  "peerDependencies": {
    "tslint": "~5.8.0",
    "typescript": "~2.6.1"
  },
  "dependencies": {
    "comment-json": "~1.1.3"
  }
}
