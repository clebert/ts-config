{
  "name": "ts-config",
  "version": "0.0.0-development",
  "description": "Expandable, well-designed configurations for the latest versions of TSLint and TypeScript.",
  "license": "MIT",
  "author": "Clemens Akens <clebert@me.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/clebert/ts-config.git"
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-customizable"
    },
    "cz-customizable": {
      "config": ".cz-config.js"
    }
  },
  "scripts": {
    "commitmsg": "conventional-changelog-lint --edit",
    "cz": "git-cz",
    "release": "semantic-release pre && npm publish && semantic-release post",
    "test": "conventional-changelog-lint --from=HEAD~1 && npm run test:tslint && npm run test:tslint-rules && npm run test:tslint-type-check && npm run test:tslint-type-check-rules && npm run test:typescript",
    "test:tslint": "tslint --config tslint.json test.ts",
    "test:tslint-rules": "! tslint --config tslint.json test.ts 2>&1 | grep -q 'Could not find implementations'",
    "test:tslint-type-check": "tslint --config tslint.json --project tsconfig.json --type-check test.ts",
    "test:tslint-type-check-rules": "! tslint --config tslint.json --project tsconfig.json --type-check test.ts 2>&1 | grep -q 'Could not find implementations'",
    "test:typescript": "tsc --project ."
  },
  "devDependencies": {
    "commitizen": "2.9.6",
    "conventional-changelog-lint": "1.1.9",
    "cz-customizable": "5.0.0",
    "husky": "0.13.3",
    "semantic-release": "6.3.2",
    "tslib": "1.6.1",
    "tslint": "5.1.0",
    "typescript": "2.2.2"
  },
  "peerDependencies": {
    "tslib": "1.6.x",
    "tslint": "5.1.x",
    "typescript": "2.2.x"
  }
}
